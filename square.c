#include<stdio.h>
#include<math.h>

float squareArea(flaot side);
float circleArea(flaot rad);
float rectangleArea(flaot a, float b);

int main() {
  

    return 0;
}

float sqaureArea(float side) {
    return side*side;
}

float circleArea(float rad) {
    return 3.14 * rad * rad;
}

float rectangleArea(float a, float b) {
    return a * b;
}